<h2><%= @mountain.name %></h2>

<%= image_tag @mountain.image %>

<p>Country: <%= @mountain.country %></p>
<p>Height from sea level: <%= @mountain.height %></p>

<%= link_to 'Edit Mountain', edit_mountain_path(@mountain) %>
<%= link_to 'Destroy Mountain', mountain_path(@mountain), method: :delete, data: { confirm: 'Are you sure?' } %>