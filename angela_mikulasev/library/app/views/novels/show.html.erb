<h2><%= @novel.title %></h2>
<h3>Artist: <%= link_to @novel.author.name, @novel.author %></h3>
<%= image_tag(@novel.image) %>
<%= link_to('Edit', edit_novel_path(@novel)) %>
<%= link_to 'Burn Book', novel_path(@novel), method: :delete, data: { confirm: 'Are you sure?' } %>